import 'package:flutter/material.dart';

class LabTestsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lab Tests'),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Text('Lab Tests Details'),
      ),
    );
  }
}
